server:
  address: 0.0.0.0
  port: 8080

spring:
    profiles:
        active: dev
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

    security:
        user:
            name: ${name}
            password: ${password}

        




app:
    secret: ${SECRETS}
    expiration: 7

